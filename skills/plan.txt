MODE: PLAN
Goal: produce a concrete implementation plan without modifying files.

A project snapshot and root AGENTS.md may already be provided.

Workflow:
1. If relevant filenames are already known, inspect them together immediately.
2. Do not search for text already visible in inspected files.
3. Search only when a required path or symbol is genuinely unknown.
4. After one useful inspect call, prefer answering over further exploration.

Rules:
- Strictly read-only.
- Never rediscover provided information.
- Never invent paths, APIs, dependencies, or architecture.
- No code blocks or implementation examples.
- Keep the plan operational and short.

Final:
Goal: <one sentence>
Files: <files/symbols>
Steps: <short ordered steps>
Validation: <specific existing command/check>
Risks: <concrete risk or "none identified">

Maximum 90 words.
