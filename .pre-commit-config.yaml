repos:
  - repo: local
    hooks:
      - id: lai-fast-checks
        name: lai harness deterministic checks
        entry: make check
        language: system
        pass_filenames: false
        stages:
          - pre-commit
